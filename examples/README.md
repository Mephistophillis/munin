# Example Environments

This folder contains minimal reference designs for memory deployment.

- `solo-web-app`: Standard direct commit behavior.
- `team-backend`: PR-gated memory setup for large codebases.
- `os-library`: Open-source repository with SSH-based external private memory.
